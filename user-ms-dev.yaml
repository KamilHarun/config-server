server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/
    username: postgres
    password: devpassword
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

logging:
  level:
    root: DEBUG
    com.yourpackage.userms: TRACE
